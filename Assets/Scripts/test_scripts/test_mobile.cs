using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test_ : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Input.acceleration
        //Handheld.Vibrate
        //Input.touches    everytimes the finger touch the screen, it triggers a touch event, up to 5 maximum
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
